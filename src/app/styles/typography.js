import typography from '@economist/component-typography/lib/typography';

const text = Object.assign(
  {
    // Base text size on a page. To style page elements use relative sizes from below
    baseSize: '18px',
  },
  typography
);
export default text;
